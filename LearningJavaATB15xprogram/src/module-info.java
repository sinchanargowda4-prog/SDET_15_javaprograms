/**
 * 
 */
/**
 * 
 */
// module-info.java disabled because this project is built and run on the classpath
// (TestNG and many test dependencies are not modularized in the same way).
// If you want to use the Java module system, recreate a proper module-info with only
// modular dependencies and ensure all required libraries are available as modules.
